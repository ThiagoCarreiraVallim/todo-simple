{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "go run ./cmd/api",
    "build": "go build -o bin/api ./cmd/api",
    "lint": "gofmt -l . && go vet ./...",
    "test": "go test ./...",
    "migrate": "go run ./cmd/migrate",
    "migrate:down": "go run ./cmd/migrate -down"
  }
}
